@model Reservation
<h2>Create a New Reservation</h2>
@using (Html.BeginForm("InsertReservationToDatabase", "Reservation", FormMethod.Get))
{
    <input asp-for="ID" type="hidden" value="" />
    <label asp-for="Date" class="control-label">Date</label>
    <input type="text" name="Date" class="form-control" value="" />
    <label asp-for="Time" class="control-label">Time</label>
    <input type="number" step="0.01" name="Time" class="form-control" />
    <div class="form-group">
        <label for="">Select a Time Slot</label>
        <select class="form-control" name="ID" value="">
            @foreach (var res in Model.TimeSlots)
            {
                <option value="@res.ID">@res.ID @res.Time</option>
            }
            <a href="C:\Users\amori\repos\RestaurantReservation\ReservationApp\Views\_ViewStart.cshtml">C:\Users\amori\repos\RestaurantReservation\ReservationApp\Views\_ViewStart.cshtml</a>
        </select>
    </div>
    <input type="submit" value="Create Reservation" class="btn btn-primary" />
}
